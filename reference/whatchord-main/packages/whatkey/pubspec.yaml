name: whatkey
description: Streaming key detection from live chord evidence for WhatChord.
homepage: https://github.com/EarthmanMuons/whatchord/tree/main/research/whatkey
repository: https://github.com/EarthmanMuons/whatchord/tree/main/packages/whatkey
issue_tracker: https://github.com/EarthmanMuons/whatchord/issues

publish_to: none
version: 0.1.0
resolution: workspace

environment:
  # Library floor: the oldest SDK this package works on. Widen or raise
  # deliberately; the app pins its own toolchain separately.
  sdk: ^3.8.0

dependencies:
  meta: ^1.17.0
  whatchord: ^0.1.0

dev_dependencies:
  import_order_lint: ^0.2.2
  lints: ^6.0.0
  test: ^1.25.0
