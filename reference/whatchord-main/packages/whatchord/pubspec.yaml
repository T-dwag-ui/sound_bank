name: whatchord
description: Chord identification and harmony analysis engine for WhatChord.
homepage: https://whatchord.earthmanmuons.com/
repository: https://github.com/EarthmanMuons/whatchord/tree/main/packages/whatchord
issue_tracker: https://github.com/EarthmanMuons/whatchord/issues

publish_to: none
version: 0.1.0
resolution: workspace

environment:
  # Library floor: the oldest SDK this package works on (null-aware
  # elements require 3.8). Widen or raise deliberately; the app pins its
  # own toolchain separately.
  sdk: ^3.8.0

dependencies:
  collection: ^1.19.0
  meta: ^1.17.0

dev_dependencies:
  import_order_lint: ^0.2.2
  kiri_check: ^1.3.1
  lints: ^6.0.0
  test: ^1.25.0
