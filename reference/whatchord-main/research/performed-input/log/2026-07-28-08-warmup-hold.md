# 2026-07-28: Feel-test finding: warmup blank bridged by holding the display

**Goal.** Fix the first in-practice finding on the gated display (log -07):
building a chord note by note (C, then E, then G) flashed the waiting
placeholder for the gate's stability window between the interval label and the
chord label.

**What happened.** The third note flips the analysis mode to chord before the
gate has promoted anything, so the chord branch yielded null and the card fell
back to the placeholder for ~200 ms. Fix: `heldIdentityDisplayProvider` wraps
the identity display with warmup continuity. While any notes sound, the last
shown label (note, interval, or chord) holds through the gate's window; the
display clears only when nothing sounds. This holds a stable stale label, not
the raw stream, so the rejected immediate-onset flicker (log -07) stays
rejected. Both home sections consume the held provider; writes land between
passes per the same-frame rebuild rule. Test added for the
note-interval-chord-silence sequence; full root suite green.

**Plain-English reading.** Stacking a chord one note at a time now reads C, then
major third, then C major, with no "waiting" blink in between: the previous
label simply stays put for the fifth of a second the chord needs to prove
itself.

**Next.** Continue feel-testing; the held display is the expected shape for any
further warmup-continuity findings.
