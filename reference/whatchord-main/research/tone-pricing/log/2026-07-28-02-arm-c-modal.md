# 2026-07-28: Arm C rebuilt on modal configurations; the residual instrument is clean

**Goal.** Build-order step 1: replace arm C's union-with-threshold span voicings
(which combined tones that never sounded together and inflated the superset
bucket, performed-input log 2026-07-27-07) with simultaneity-preserving
construction, so the BC residual this initiative aims at stops measuring its own
artifacts.

**Setup.** `replay_batch.dart`'s span segmentation now analyzes each span's
modal configuration: the actually-simultaneous sounding set (3 or more notes)
with the longest total dwell inside the span. The threshold plumbing is removed;
manifests record `spanVoicing: modal-configuration`. Arms C and BC regenerated;
scored on the rescued 23-movement development roster (not comparable to the
older 21-piece tables).

**What happened.**

| arm      | coverage | exact | root  | members |
| -------- | -------- | ----- | ----- | ------- |
| C-modal  | 0.923    | 0.574 | 0.736 | 0.505   |
| BC-modal | 0.923    | 0.574 | 0.736 | 0.505   |

- Coverage jumps from 0.75 (union) to 0.92: nearly every analyst span contains a
  real simultaneous sonority, so the residual instrument now covers almost all
  annotated time.
- The artifact is gone. The BC error census's playable share falls from 0.398
  (union) to 0.203, nearly matching the real capture path's 0.239, and the
  appSuperset cell collapses from 0.238 to 0.098. BC's disagreement now
  decomposes like A0's (playable 0.203, partial 0.470, absent 0.327), which is
  what an honest perfect-segmentation arm should look like.
- B and BC remain identical to three decimals: the context-free finding holds on
  the refined arm.

**The initiative's residual baseline**, then: at perfect boundaries and perfect
key, on real simultaneous sonorities, the engine agrees with the analyst's root
and quality family 57.4% of covered time, with the engine-attention (playable)
slice at about a fifth of disagreement. This is the number the pricing levers
will be measured against.

**Plain-English reading.** The perfect-segmentation measuring stick used to
grade the engine on note piles that nobody ever played at the same time. It now
grades on real moments, covers nearly all the annotated music instead of three
quarters, and its errors finally look like the live app's errors, so when a
pricing change moves this number we can believe the movement.

**Next.** Build-order step 2 continues: the 8-plus pitch-class self-consistency
census baseline and the standing-row inventory, then the experiment-mechanism
scoping.
