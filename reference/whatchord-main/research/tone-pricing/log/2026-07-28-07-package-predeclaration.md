# 2026-07-28: Pre-declaration: the modest package (m(maj7) tier + utc 1.0)

**Goal.** Pre-declare the combined package from log -06 before any result is
seen: minorMajor7 promoted from marked (0.1) to uncommon (0.4) on frequency
grounds, and the unexplained-tone price lowered from 2.0 to 1.0 under its
revised claim (partial-content base naming).

**Implementation constraints, stated up front.**

- Prices become profile-aware: the frozen whatKeyPaper2026 profile keeps utc 2.0
  and m(maj7) at the marked price, so the paper fixtures and the historical
  reproduction contract regenerate byte-identically. The current profile carries
  the package. Byte-identity of the paper-profile ruler set is verified as part
  of the run.
- The tier move touches `vocabularyTier` only: m(maj7) remains a marked
  extension host (its extension surcharge is a separate explicit list), and it
  is not rare-tier, so the dominant-slash rule is unaffected.
- Evaluation compares current-profile arm sets generated before and after the
  change from the same code lineage, avoiding the tie-rule confound with the
  paper-profile ruler baselines.

**Pre-declared expectations.**

1. Ruler (current-profile A0 and BC-modal, development): exact improves with
   CI95 excluding zero, magnitude near the sweep's utc-1.0 gains (+0.004 to
   +0.009), the tier move adding at the margins. Coverage unchanged. The claimed
   bucket: partial-content base naming, plus a visible shave of the m(maj7)
   absorption slice in the census.
2. Standing rows: predicted zero flips (their cases carry full-explanation
   readings; none has an m(maj7) top). Any flip triggers review-on-flip.
3. Dense census, both corpora on current-profile fixtures: consistency not
   materially below the baselines (0.9755 pop, 1.0000 classical thin).
4. Pool blast radius (current engine, before versus after): flips reported count
   and exposure-weighted; expected concentration in m(maj7)-adjacent and
   unexplained-tone near-ties; any reviewed-entry flip re-verdicted before ship
   (review-on-flip). Exposure touched expected small (m(maj7) family carries
   about 1.3% of observed mass as a case class).
5. Guards: solo goldens, comping suite 18/18, benchmark check green; package and
   root suites green; whatKeyPaper2026-profile ruler set byte-identical after
   the change (the whatkey non-interference proof).
6. Stated failure modes: legitimate m(maj7) namings in pop and jazz idioms
   losing to worse names (watch the pop dense census quality mix and any
   goldens); the utc side re-triggering laziness (root rising while exact falls
   anywhere).

**Ship-or-revert rule.** All of 1 to 5 green ships the package with a changelog
entry; any failure reverts the engine edit and records the numbers.
